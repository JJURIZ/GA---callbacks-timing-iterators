function sayHello(name) {
    let msg = 'Hello, ' + name + '. How are you?';
    return msg;
}

console.log(sayHello('SEI 1019'));

function sayHello(name) {
    let msg = 'Hello, ' + name + '. How are you?';
    return msg;
}

console.log(sayHello('Michael'));

function sayHello(name) {
    let msg = 'Hello, ' + name + '. How are you?';
    return msg;
}

console.log(sayHello('Tilly'));

function sayHello(name) {
    let msg = "Hello, " + name + '. How are you?';
    return msg;
};

console.log(sayHello('Detmer'));

function sayHello(name) {
    let msg = "Hello, " + name + '. How are you?';
    return msg;
}

console.log(sayHello("Saru"));